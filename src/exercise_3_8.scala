class exercise_3_8 {
  // It creates another list with it's elements pointing to the same elements as original list

  // Actual book answer mentions we get back original list. I'm curious if in practice is the exact original list or if
  // a different list but with the entries point to the original entries given.

  // Actual book answer also mentions the other way of thinking about foldRight that is it replaces the Nil constructor
  // of the list with the Z argument, and the cons constructor with the given function.
  // Answer also features a full evaluation trace.
}
